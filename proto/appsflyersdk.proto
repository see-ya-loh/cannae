syntax = "proto3";

package AppsFlyerSDK;

enum DeepLinkError {
  TIMEOUT = 0;
  NETWORK = 1;
  HTTP_STATUS_CODE = 2;
  UNEXPECTED = 3;
}

enum DeepLinkStatus {
  FOUND = 0;
  NOT_FOUND = 1;
  ERROR = 2;
}

enum EmailCryptType {
  EmailCryptTypeNone = 0;
  EmailCryptTypeSHA256 = 1;
}
