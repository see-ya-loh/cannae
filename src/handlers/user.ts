import * as protocol from "../types/cannae_protocol"



var fullTemplate: Response[] = [
    
] 

export function handleUserLogin(req: Request) {
/*     var now = Date.now()
    var serverTime = {
        now: now.toString(),
        utc_now: (now - 32400).toString(),
        time_gap: 7200
    } satisfies protocol.RspServerTime
    var clanAttendance = {
        state: "FS_NotOpen"
    }
    var userTreasury = {
        start_date: 1777066104
    } */
    return
}